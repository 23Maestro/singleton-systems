{
  "name": "@singleton-systems/lineups-nfl-rolodex",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "build": "esbuild src/code.ts --bundle --format=iife --platform=browser --target=es2020 --outfile=dist/code.js",
    "bridge": "node server.mjs",
    "check": "tsc --noEmit",
    "test": "node --test tests/*.test.mjs",
    "verify": "npm run check && npm run test && npm run build"
  },
  "devDependencies": {
    "@figma/plugin-typings": "^1.121.0",
    "@types/node": "^20.19.43",
    "esbuild": "^0.25.8",
    "typescript": "^5.7.2"
  }
}
